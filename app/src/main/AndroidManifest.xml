<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.sanitization.sanitization">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name="in.sanitization.sanitization.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="in.sanitization.sanitization.PackageDetails"></activity>
        <activity android:name="in.sanitization.sanitization.NewActivity" />
        <activity android:name="in.sanitization.sanitization.DemoActivity" />
        <activity android:name="in.sanitization.sanitization.MainActivity" />
        <activity android:name="in.sanitization.sanitization.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>